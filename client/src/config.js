const config = {
  todosUrl: "https://react-web-todo.herokuapp.com/api/todos",
  buttonDefaultStyle: {
    width: "2rem",
    height: "1rem",
    borderRadius: "0.25rem"
  },
  doneStatusColor: "#27ae60",
  doneTextDecoration: "line-through",
  todoStatusColor: "#222",
  todoTextDecoration: "none",
  themeColor: "#428eda"
};

export default config;
